# Railway-specific deploy hint — static-only.
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "caddy run --config Caddyfile --adapter caddyfile"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
